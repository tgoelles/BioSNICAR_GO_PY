name: base
channels:
  - conda-forge
  - defaults
dependencies:
  - xarray
  - dask
  - netCDF4
  - bottleneck
  - scipy
  - pip
  - pip:
      - miepython
      - statsmodels
      - seaborn
      - plotnine
      - pre-commit
      - streamlit
      - plotly
prefix: /opt/conda
